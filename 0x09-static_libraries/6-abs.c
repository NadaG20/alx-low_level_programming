#include "main.h"

/**
 * _abs - computes the absloutet value of an integer
 * @n: the int to check
 * Return: the absloute value of int
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}
